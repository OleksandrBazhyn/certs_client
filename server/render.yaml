services:
  - type: web
    name: certs-server
    env: docker
    repo: https://github.com/OleksandrBazhyn/certs_client
    dockerfilePath: server/Dockerfile
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001